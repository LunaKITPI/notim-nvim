-- when adding a custom plugin, you can add the config in a new lua file and then add it here with the same syntax as the norg example
-- ofcourse you can also just add the config in your .config file if that's what you prefer
require("custom.opts.norg")
